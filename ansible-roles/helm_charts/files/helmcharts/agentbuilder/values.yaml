global:
  partOf: app

replicaCount: 1

image:
  repository: luthersystems/agent-builder
  tag: latest
  pullPolicy: IfNotPresent

mcp:
  transport: "http"
  addr: ":8080"
  path: "/mcp"
  baseUrl: "https://insideout.luthersystemsapp.com"

# Environment variables for the agent-builder server
env:
  MCP_VERBOSE: "true"
  MCP_DEVTOOLS_ENABLED: "false"

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources: {}
imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""
nodeSelector: {}
tolerations: []
affinity: []

serviceAccount:
  create: true
  annotations: {}
  name: ""

ingress:
  enabled: false
  annotations: {}
  hosts:
    - host: chart-example.local
      paths: []
  tls: []

