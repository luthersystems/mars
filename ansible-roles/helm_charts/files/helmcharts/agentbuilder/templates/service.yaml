apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentbuilder.fullname" . }}
  labels:
    {{- include "agentbuilder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentbuilder.labels.match" . | nindent 4 }}

