apiVersion: v1
kind: Service
metadata:
  name: {{ include "connectordiscoverymcp.fullname" . }}
  labels:
    {{- include "connectordiscoverymcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "connectordiscoverymcp.labels.match" . | nindent 4 }}

